# Логи

## dmesg

```shell
dmesg -WH
```

-H (или --human) преобразует непонятные секунды с момента загрузки в привычные, читаемые человеком временные метки.
-W (или --follow-new) — это магия. Это как tail -f для вашего ядра, печатает новые сообщения по мере их появления.
